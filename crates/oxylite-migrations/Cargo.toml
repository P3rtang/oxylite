[package]
license = "Apache-2.0"
name = "oxylite-migrations"
version = "0.1.4"
edition = "2024"
description = "embed a consumer's migrations/ directory at compile time: oxylite::migrations!(\"migrations\")"
repository = "https://github.com/P3rtang/oxylite"
keywords = ["offline-first", "sync", "postgres", "pglite", "migrations"]

[lib]
proc-macro = true
