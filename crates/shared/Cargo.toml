[package]
license = "Apache-2.0"
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
enum-iterator = "2"
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["serde", "v7", "js"] }
serde_json = "1"
