# web-sys gates newer browser APIs (Web Locks: navigator.locks) behind
# this cfg — required for the cross-tab leader election (docs/impl/
# multi-tab.md). Harmless on host builds; web-sys is bindings only.
[build]
rustflags = ["--cfg=web_sys_unstable_apis"]
